# EduMentor 环境变量模板 —— 复制为 .env 后填写真实 Key
# 开发期（笔记本）：FAISS + 内存缓存 + 云端 DeepSeek/千问
# 生产期（Ubuntu+4090）：Milvus + Redis + 本地 Ollama（切换下方取值，见 docs/07 §8.3）

# --- LLM 提供商：deepseek(云) | local(Ollama) ---
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-你的DeepSeekKey
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-v4-flash
LOCAL_LLM_BASE_URL=http://localhost:11434/v1
LOCAL_LLM_MODEL=qwen2.5:14b

# --- Embedding 提供商：qwen(云) | local(Ollama) ---
EMBEDDING_PROVIDER=qwen
QWEN_API_KEY=sk-你的千问Key
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_EMBEDDING_MODEL=text-embedding-v4
LOCAL_EMBEDDING_BASE_URL=http://localhost:11434/v1
LOCAL_EMBEDDING_MODEL=bge-m3

# --- 业务库（MySQL，pymysql 驱动） ---
DATABASE_URL=mysql+pymysql://edumentor:edumentor123@127.0.0.1:3306/edumentor?charset=utf8mb4

# --- 向量库后端：faiss(开发) | milvus(生产) ---
VECTOR_BACKEND=faiss
VECTOR_INDEX_DIR=data/vector_index
MILVUS_HOST=127.0.0.1
MILVUS_PORT=19530

# --- 缓存后端：memory(开发) | redis(生产) ---
CACHE_BACKEND=memory
CACHE_URL=redis://127.0.0.1:6379/0

# --- 应用 ---
APP_NAME=EduMentor
APP_VERSION=0.1.0
APP_PORT=8000
UPLOAD_DIR=data/uploads
APP_ENV=dev

# --- 鉴权 JWT（生产环境请更换为随机长字符串） ---
JWT_SECRET=dev-secret-change-me
JWT_EXPIRE_SECONDS=86400
